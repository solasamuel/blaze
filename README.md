# blaze
blaze is a command-line HTTP load testing tool. It fires a configurable number of concurrent requests at a target endpoint, measures the latency of each, and reports percentiles (p50, p95, p99), throughput, and error rates. While running, it displays a live updating terminal view of progress and current statistics.
